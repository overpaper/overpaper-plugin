import { $bodyInline } from "./body";
import { $button } from "./button";
import { $form } from "./form";
import { $icon } from "./icon";
import { $input } from "./input";
import { $link } from "./link";
import { $oauth } from "./oauth";
import { $text } from "./text";

export const $el = {
  text: $text,
  link: $link,
  icon: $icon,
  button: $button,
  input: $input,
  form: $form,
  oauth: $oauth
};

export const $body = {
  inline: $bodyInline
};
